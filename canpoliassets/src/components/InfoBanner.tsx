/*
 * A banner that displays different snippets of information like stories from
 * the press that are relevant, or milestones in our communities project.
 */

import { Box } from "@mui/material";

function InfoBanner() {

    return (
        <Box>
        </Box>
    );
}

export default InfoBanner;
